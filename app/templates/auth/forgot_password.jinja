<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Recuperar contraseña</title>
    <link rel="stylesheet" href="{{ url_for('static', filename='css/login.css') }}">
</head>

<body>
    <div class="login-container">
        <div class="avatar">
            <img src="{{ url_for('static', filename='assets/avatar/key_115188.png') }}" alt="Avatar">
        </div>
        <h2>Recuperar Contraseña</h2>

        <!-- Contenedor para mensajes -->
        <div id="alert-container">
            {% with messages = get_flashed_messages(with_categories=True) %}
            {% if messages %}
            {% for category, message in messages %}
            <div class="alert alert-{{ category }}">
                {{ message }}
            </div>
            {% endfor %}
            {% endif %}
            {% endwith %}
        </div>

        <!-- Formulario -->
        <form id="forgotPasswordForm" action="" method="POST" 
            data-email-url="{{ url_for('auth.forgot_password') }}"
            data-phone-url="{{ url_for('auth.forgot_password_phone') }}">

            <div class="form-group">
                <label for="recoveryMethod">Selecciona el método de recuperación:</label>
                <select id="recoveryMethod" name="recoveryMethod" required>
                    <option value="">-- Selecciona una opción --</option>
                    <option value="email">Correo Electrónico</option>
                    <option value="phone">Número de Teléfono</option>
                </select>
            </div>

            <div id="emailForm" class="form-group" style="display: none;">
                <label for="email">Correo Electrónico</label>
                <input type="email" id="email" name="email" placeholder="Ingresa tu correo">
            </div>

            <div id="phoneForm" class="form-group" style="display: none;">
                <label for="phone">Número de Teléfono</label>
                <input type="tel" id="phone" name="phone" placeholder="Ingresa tu número de teléfono">
            </div>

            <div id="questionsForm" class="form-group" style="display: none;">
    <label for="email">Correo Electrónico</label>
    <input type="email" id="email" name="email" placeholder="Ingresa tu correo">
</div>

            <button type="submit" class="btn-login">Enviar enlace/código de recuperación</button>
        </form>

        <p>¿Ya tienes una cuenta? <a href="{{ url_for('auth.login') }}">Inicia Sesión</a></p>

        <script src="{{ url_for('static', filename='js/opcioncontraseña.js') }}"></script>
</body>

</html>